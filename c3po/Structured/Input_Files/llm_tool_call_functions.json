[
    {
        "type": "function",
        "function": {
            "name": "calculate_date_ranges",
            "description": "This function DOES NOT EXECUTE ANY PYTHON CODE. It is used STRICTLY after retrieving the latest date. You must pass that LATEST DATE (in '%Y-%m-%d' format) as the parameter to this function ONLY when the question is related to calculating date ranges for Q1TD, Q2TD, Q3TD, Q4TD, R13W, P13W, R4W, P4W, R12M, and P12M. This function will return a dictionary of the calculated date ranges. Do not embed any Python execution logic inside this function call or assume any random date. ALWAYS FIRST to determine the latest date from the dataset, and then use that date for calculating the date ranges here.",
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "A single date string in the format '%Y-%m-%d' representing the LATEST DATE for which we want to compute various date ranges."
                    }
                },
                "required": ["query"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "generate_sql_code",
            "description" : "Useful when you need to write and run the sql query for data analysis. This function is for executing the SQL query to figure out the LATEST DATE from the dataset only when the question pertains to date ranges related to Q1TD, Q2TD, Q3TD, Q4TD, R13W, P13W, R4W, P4W, R12M, and P12M. and perform other data manipulations to answer the user question. The code generated here must not contain any logic that involves directly calling calculate_date_ranges. Instead, you will first use this function to get the latest date, and then pass it as a parameter to the calculate_date_ranges function. The code to be run should be a stand-alone snippet and should not be dependent on the calculate_date_ranges function call unless the question specifically relates to these date ranges.",
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "The SQL code you want to execute, for instance, to find the latest date from a dataset or to do advanced data analysis/processing. Must be a valid SQL Query, but should NOT include calculate_date_ranges or other conflicting logic, unless the question is related to the date ranges of Q1TD, Q2TD, Q3TD, Q4TD, R13W, P13W, R4W, P4W, R12M, and P12M."
                    }
                },
                "required": ["query"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "python_after_sql",
            "description": "This function is for python handling after sql execution to handle visualization and complex filteration that can't handled by sql. Strictly use this function after the sql query is executed and the result is obtained. Never forget to use this function after the sql query is executed and the result is obtained.",
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "The Python code used after SQL handles visualizations and complex filtering that SQL cannot manage.when using this tool STRICTLY Always import the libraries required in the code.ALWAYS ASSUME THE LIBRARIES ARE NOT IMPORTED AND IMPORT AT THE BEGINING OF THE CODE."
                    }
                },
                "required": ["query"]
            }
        }
    },
    {
        "type": "function",
        "function": {
            "name": "insights_response",
            "description": "This function is all about to providing insights. It is must used after completing the visualization part. You must call this function to get data to provide rich insights. Function should be called for every completion of visualization task.",
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "Function call to get data and provide insights"
                    }
                },
                "required": ["query"]
            }
        }
    }
]

