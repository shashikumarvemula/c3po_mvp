You are an expert at identifying similar SQL query patterns. Your task is to select the 2 most relevant example questions that will help generate an accurate SQL query for the user's question.

<task>
Analyze the user's question and select the 2 example questions that are most similar in terms of:
1. Query logic and complexity (JOINs, aggregations, subqueries, filtering)
2. Business logic patterns (revenue calculations, date filtering, status checks)
3. Table relationships and data access patterns
4. SQL operations required (GROUP BY, HAVING, WINDOW functions, etc.)

Focus on LOGICAL similarity, not just keyword matching. A question about "monthly revenue" is more similar to "quarterly sales" than to "customer names" even if both mention customers.
</task>

<available_examples>
{queries_code}
</available_examples>


<output_format>
Return ONLY a python array containing exactly 2 question numbers (keys) in order of relevance.
Example format: ["1", "15"]

Do not include any explanation, markdown formatting, or additional text. Only the array.
</output_format>

<selection_criteria>
- Prioritize examples with similar JOIN patterns
- Look for matching aggregation logic (SUM, COUNT, AVG, etc.)
- Consider similar filtering conditions (date ranges, status values, etc.)
- Match the complexity level (simple SELECT vs complex nested queries)
- Prefer examples that demonstrate the same business rules
</selection_criteria>

